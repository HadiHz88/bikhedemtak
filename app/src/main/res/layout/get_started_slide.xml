<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <TextView
        android:id="@+id/titleSlide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Slide Text"
        android:textSize="24sp"
        android:gravity="center"
        android:layout_marginTop="16dp"
        android:textColor="@color/soft_pink"/>

    <TextView
        android:id="@+id/descSlide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Slide Description"
        android:textSize="14sp"
        android:gravity="center"
        android:layout_marginTop="8dp"
        android:textColor="@color/dark_blue"/>

    <!-- Use a FrameLayout to contain the circle and image -->
    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp">

        <!-- Circle background with fixed size -->
        <View
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:background="@drawable/circle_background"/>

        <!-- ImageView to fit inside the circle -->
        <ImageView
            android:id="@+id/imageSlide"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/ic_launcher_foreground"
            android:contentDescription="@string/app_name"
            android:layout_gravity="center"
            android:clipToOutline="true"/>
    </FrameLayout>

</LinearLayout>